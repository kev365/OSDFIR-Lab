# OS artifacts
.DS_Store
Thumbs.db
desktop.ini
*.ps1.bak
*.tmp
*.temp
~*
*.bak
*.backup

# Project folders
.cursor
.taskmaster
.taskmaster/
backups
scripts/dev
scripts/dev/
commands.md
.cursorignore
.claude

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
*.tfplan
.terraform.lock.hcl
.terraform.tfstate.lock.info
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Kubernetes
kubeconfig
.kube/

# Helm
charts/*.tgz
.helm/
# Terraform-side chart cache (populated by Invoke-HelmPull)
terraform/chart-cache/
# Generated openrelik.workers override (rebuilt from the catalog on every deploy)
configs/osdfir-lab-workers.generated.yaml

# Docker
.docker/

# AI model cache (if downloaded locally)
models/
*.gguf

# Jupyter notebooks checkpoints
.ipynb_checkpoints/

# Python cache (if any Python tools are added)
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
pip-log.txt
.venv/
venv/

# Node modules (if any frontend tools are added)
node_modules/
npm-debug.log

# Generated Timesketch config tarball
ts-configs.tar.gz
helm-addons/files/*.tgz
helm-addons/files/*.tar.gz
